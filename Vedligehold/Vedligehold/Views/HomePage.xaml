<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Vedligehold.Views.HomePage"
                         Title="Startside">


  <StackLayout Padding="10"
               VerticalOptions="Center">

    <!--<Entry x:Name="MainEntry"
            Placeholder="Indtast leverandørnummer (103)"
           Keyboard="Numeric"/>-->

    <!--<Button Clicked="Button_OnClicked"
            Text="Say Hi!"
            BackgroundColor="Green"
            TextColor="White" />

    <Label x:Name="MainLabel"
           TextColor="Gray"
           FontSize="60"/>-->

    <Button x:Name="StatButton"
            Text="Se biomassestatistikker"
            BackgroundColor="Green"
            TextColor="White"></Button>

    <Button x:Name="MaintButton"
         Text="Se vedligeholdsopgaver"
         BackgroundColor="Green"
         TextColor="White"></Button>
    <Button x:Name="LogOutButton"
             Text="Log ud"
             BackgroundColor="Green"
             TextColor="White"></Button>
    
    <Image x:Name="image"></Image>
    <Label x:Name="lbl"></Label>


  <ProgressBar x:Name="MainProgressBar"
               Progress="0"
               VerticalOptions="Center"
               HorizontalOptions="Center">
  </ProgressBar>

    <ListView x:Name="StatList"
               HasUnevenRows="True">
    </ListView>
  </StackLayout>



</ContentPage>